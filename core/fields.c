#include "fields.h"

field *fields_calculate_scalar(int N, float L, float *x, float *s, int S){
/*    field *rho = (field*)malloc(sizeof(field));

    rho->size[0]  = S;
    rho->size[1]  = S;
    rho->size[2]  = (DIM>2)?S:1;
    rho->numcomps = 1;
    rho->data  = (float*(malloc(sizeof(float)*
*/
}


